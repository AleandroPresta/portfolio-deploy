// A Skill class with two attributes, name and experience level
export class Skill {
    name: string;
    experience: string;
    constructor(name: string, experience: string) {
        this.name = name;
        this.experience = experience;
    }
}